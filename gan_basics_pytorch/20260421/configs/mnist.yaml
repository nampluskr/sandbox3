# configs/mnist.yaml

seed: 42

path:
  data_dir: /home/namu/myspace/NAMU/datasets/mnist
  output_dir: outputs/mnist

data:
  batch_size: 128
  num_workers: 4
  img_size: 32
  in_channels: 1
  out_channels: 1
